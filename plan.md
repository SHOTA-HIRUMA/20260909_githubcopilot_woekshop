# Pomodoro Timer Webアプリ 段階的実装計画

このドキュメントは、UIモック、アーキテクチャ方針、機能一覧を踏まえて、実装を段階的に進めるための計画を示す。

## 1. 進め方の原則

- 1フェーズごとに「動く成果物」を必ず作る
- 1タスクは半日以内に完了判定できる粒度に分割する
- ドメインロジックとDOM操作を同一タスクで混在させない
- 機能追加時は最小テストを先に用意する
- 仕様変更時は先に features.md を更新してから実装する

## 2. フェーズ計画

### Phase 0: 土台構築（0.5日）

目的:

- 以降の実装を安全に進める最小基盤を用意する

実装内容:

- Flaskアプリ起動
- ルート画面配信
- 静的ファイル構成（HTML/CSS/JS）準備
- ヘルスチェックAPI追加

完了条件:

- ブラウザでトップページ表示ができる
- ヘルスチェックがHTTP 200を返す

テスト観点:

- ルーティング疎通
- サーバー起動確認

### Phase 1: UIモック再現（1日）

目的:

- 添付モックに近い静的UIを先に完成させる

実装内容:

- レイアウト実装
- タイトル、モード表示、タイマー表示、操作ボタン、進捗カード
- 円形プログレスリング（静的）
- モバイル/デスクトップ向けレスポンシブ調整

完了条件:

- モックに近い見た目を再現できる
- 主要解像度でレイアウト崩れがない

テスト観点:

- 主要UI要素の存在確認
- 画面幅別の表示確認

### Phase 2: タイマーコア（ドメイン）実装（1日）

目的:

- ユニットテストしやすい中核ロジックを独立実装する

実装内容:

- 状態モデル（Idle/Running/Paused/Finished）
- モードモデル（Work/ShortBreak/LongBreak）
- イベント処理（Start/Pause/Resume/Reset/Tick）
- 終了予定時刻ベースの残り時間計算
- Clock抽象化（SystemClock/FakeClock）

完了条件:

- ロジック単体で状態遷移が成立する
- 時刻飛び（復帰時）でも残り時間が破綻しない

テスト観点:

- 状態遷移テスト
- 残り時間計算テスト
- 不正遷移拒否テスト

### Phase 3: UI連携（0.5-1日）

目的:

- 画面操作でタイマーを実際に動かせる状態にする

実装内容:

- 開始/一時停止/再開/リセットのイベント接続
- 残り時間表示更新
- リング進捗更新
- モード表示更新

完了条件:

- 1セッションを画面操作で完走できる
- UI表示と内部状態が一致する

テスト観点:

- 操作シナリオ（開始→停止→再開→完了）
- 表示更新の整合性

### Phase 4: セッション遷移と進捗集計（1日）

目的:

- ポモドーロとして必要な遷移ルールと進捗集計を完成させる

実装内容:

- セッション完了時の自動モード遷移
- 長休憩挿入ルール（例: 4回ごと）
- 当日進捗（完了回数・集中時間）の更新
- 日跨ぎリセット

完了条件:

- Work→Break遷移が仕様通りに動作する
- 長休憩タイミングが一致する
- 進捗値が正しく加算される

テスト観点:

- 長休憩挿入ルール
- 集中時間累積
- 日次リセット

### Phase 5: 永続化とAPI（1日）

目的:

- 設定と進捗を保存し、再読み込みに耐える構成にする

実装内容:

- 設定API（取得/更新）
- 当日進捗API（取得）
- セッション完了記録API
- フロント側のRepository経由の保存/復元

完了条件:

- 設定が保存・復元される
- 当日進捗が保存・復元される
- API異常時でもUIが破綻しない

テスト観点:

- API正常系/異常系
- 再読み込み復元
- バリデーション

### Phase 6: 品質強化（0.5-1日）

目的:

- MVP公開前の安定化と運用性向上を行う

実装内容:

- 入力バリデーション強化
- エラーメッセージ統一
- ログ最小実装
- テスト不足の補完

完了条件:

- 主要異常系でアプリが停止しない
- 必須機能を満たす

テスト観点:

- 異常入力
- API失敗時フォールバック
- 回帰確認

### Phase 7: 拡張機能（任意）

目的:

- 使い勝手と継続利用価値を高める

実装内容:

- 通知音
- ブラウザ通知
- キーボードショートカット
- 週間/月間分析表示

完了条件:

- 拡張機能が既存MVPを壊さず追加される

## 3. マイルストーン

- M1: Phase 0-1完了（見た目完成）
- M2: Phase 2-3完了（タイマー動作完成）
- M3: Phase 4-5完了（ポモドーロ機能と保存完成）
- M4: Phase 6完了（MVP安定化）

## 4. 直近の着手順

1. Phase 0を実装して起動確認
2. Phase 1でUIをモック準拠まで作成
3. Phase 2でタイマーコアを純粋ロジックとして実装

以上の順で進めることで、早い段階で見える成果を出しつつ、後工程のテスト性と保守性を確保できる。