# Pomodoro Timer Webアプリ 実装機能一覧

## 1. MVP（必須機能）

### 1.1 タイマー基本機能

- 作業セッションの開始
- 一時停止
- 再開
- リセット
- 残り時間表示（mm:ss）

### 1.2 セッションモード管理

- 作業モード（Work）
- 短休憩モード（Short Break）
- 長休憩モード（Long Break）
- セッション完了時の自動モード遷移
- 長休憩挿入ルール（例: 4セッションごと）

### 1.3 時間計算の信頼性

- 終了予定時刻ベースの残り時間算出
- タブ非アクティブ復帰時の補正
- スリープ復帰時の整合性維持

### 1.4 UI表示

- 現在モード表示（作業中/休憩中）
- 円形プログレスリング表示
- 開始/一時停止/リセットの操作ボタン
- 今日の進捗表示（完了回数・集中時間）

### 1.5 進捗記録

- セッション完了時の完了回数加算
- 集中時間の累積
- 日付単位の進捗管理
- 日跨ぎ時の当日進捗リセット

### 1.6 設定機能

- 作業時間の設定
- 短休憩時間の設定
- 長休憩時間の設定
- 長休憩までの回数設定

### 1.7 永続化

- 設定の保存/復元
- 当日進捗の保存/復元
- 再読み込み時の状態復元（最低限: 設定・進捗）

### 1.8 API（Flask）

- GET /api/settings（設定取得）
- PUT /api/settings（設定更新）
- GET /api/progress/today（当日進捗取得）
- POST /api/progress/session-completed（セッション完了記録）
- GET /health（ヘルスチェック）

### 1.9 エラーハンドリング

- 不正入力（負数、ゼロ、上限超え）のバリデーション
- API失敗時のユーザー通知
- 破損データ検出時のフォールバック初期化

### 1.10 テスト（最低限）

- 状態遷移テスト
- 残り時間計算テスト
- 長休憩ルールテスト
- 日次リセットテスト
- API入出力テスト

## 2. 拡張機能（優先度: 中）

### 2.1 通知

- セッション終了時の通知音
- ブラウザ通知

### 2.2 UX改善

- キーボードショートカット
- モーション/アニメーション改善
- モバイル操作性の改善

### 2.3 分析表示

- 週間/月間の集中履歴
- 平均集中時間
- 目標達成率

### 2.4 運用性

- ログ出力
- 設定のエクスポート/インポート

## 3. 将来拡張（優先度: 低）

### 3.1 ユーザー管理

- ログイン/ログアウト
- ユーザー別進捗管理

### 3.2 複数端末同期

- サーバーDBへの永続化
- デバイス間データ同期

### 3.3 チーム機能

- 共有目標
- チーム内の進捗可視化
